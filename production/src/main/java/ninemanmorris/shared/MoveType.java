package ninemanmorris.shared;

/**
 * Enum which represents the different phases in the game mapped to their quotes
 */
public enum MoveType {   
    PLACE_PHASE,
    MOVE_PHASE,
    FLY_PHASE,
    REMOVE_PHASE,
    SELECT_PHASE
}